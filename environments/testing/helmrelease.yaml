apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: frontend-testing
  namespace: default
spec:
  interval: 1m
  releaseName: frontend-testing
  chart:
    spec:
      chart: nginx
      version: "15.2.x"
      sourceRef:
        kind: HelmRepository
        name: bitnami
        namespace: flux-system
  values:
    image:
      repository: gonvazquez/frontend-testing
      tag: latest
    service:
      type: LoadBalancer
    replicaCount: 1
